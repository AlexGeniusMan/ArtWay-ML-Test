name: build

on:
  push:
    branches:    
      - 'develop'
    paths-ignore:
      - 'README.md'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Set alias
      run: . ./alias.sh
    - name: Build the docker-compose
      run: dev-amt build